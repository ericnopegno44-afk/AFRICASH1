CREATE TABLE users (id INT AUTO_INCREMENT PRIMARY KEY,name VARCHAR(120) NOT NULL,phone VARCHAR(40) NOT NULL UNIQUE,email VARCHAR(190),password_hash VARCHAR(255) NOT NULL,country VARCHAR(80),balance BIGINT NOT NULL DEFAULT 0,created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);